import{_ as e,L as a,M as s,a2 as i}from"./framework.88d00857.js";const n="/assets/20220712-宝塔面板权限.2f6f0890.png",t="/assets/20220712-配置守护进程.300e0b9d.png",d="/assets/20220712-添加反向代理.a7767f1e.png",o={},c=i('<div class="custom-container tip"><p class="custom-container-title">Tips</p><p>宝塔面板实在是太复杂了，研究了两天没搞懂，只能搞个大概的教程，本文仅供参考。</p></div><h2 id="基础环境" tabindex="-1"><a class="header-anchor" href="#基础环境" aria-hidden="true">#</a> 基础环境</h2><ul><li>Ubuntu Server 20.04</li><li>宝塔 7.9.2</li></ul><h2 id="上传可执行文件" tabindex="-1"><a class="header-anchor" href="#上传可执行文件" aria-hidden="true">#</a> 上传可执行文件</h2><p>上传可执行文件到服务器，上传位置由你自己决定，确保自己能找到，我测试的时候直接上传压缩包，然后用宝塔面板解压没有反应，不知道是我这里的 bug 还是整个宝塔面板都有问题，建议不使用压缩包，直接拖整个文件夹上去。</p><p>上传文件后检查权限，确保有执行权限，貌似宝塔上传的文件默认权限就是 755，这一步大概率可以省略，不放心的话就再检查一遍。</p><p>同时还要注意一下文件所有者，这里记一下，后面会用到，我也不知道宝塔文件所有到底是谁，面板上看是 root，但是使用 <code>ls -lha</code> 命令查看，文件所有者是 www，如果有懂的朋友可以解释一下。</p><p><img src="'+n+`" alt="宝塔面板权限" loading="lazy"></p><h2 id="修改配置文件" tabindex="-1"><a class="header-anchor" href="#修改配置文件" aria-hidden="true">#</a> 修改配置文件</h2><p>修改 <code>appsettings.json</code></p><p>需要修改的部分主要有：</p><ul><li><code>Kestrel.Endpoints.Http.Url</code>: 监听地址和端口，根据需要修改，如果只是在本机反代，无需修改，如果需要在其他机器上反代，可修改为 <code>http://0.0.0.0:5001</code></li><li><code>WithOrigins</code>: 跨域配置，如无跨域限制需求，无需修改。</li><li><code>Admins</code>: 必须修改，配置管理员账户和密码，可配置多个。</li></ul><h2 id="守护进程" tabindex="-1"><a class="header-anchor" href="#守护进程" aria-hidden="true">#</a> 守护进程</h2><p>先安装 <code>supervisor</code>，在终端执行</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> supervisor
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>以上命令仅限 Ubuntu 20.04 和 Debian11，其他系统自己去想办法解决。</p><p>然后在宝塔的软件商店找到 <code>Supervisor管理器</code>，安装完成后配置</p><p><img src="`+t+'" alt="配置守护进程" loading="lazy"></p><p>注意，截图上的可执行文件路径是错的，别 TM 不思考直接照着抄，错的都看不出来，看教程就是要举一反三，参考教程，根据自己的实际情况做出调整。</p><p>只要状态是 <code>RUNNING</code> 就正常了，其他状态自行排查错误，直接去看子配置文件，配置文件才是能直接看出错误的地方。</p><h2 id="反向代理" tabindex="-1"><a class="header-anchor" href="#反向代理" aria-hidden="true">#</a> 反向代理</h2><p>在宝塔面板上创建一个纯静态站点。</p><p>然后设置站点的反向代理。</p><p><img src="'+d+'" alt="添加反向代理" loading="lazy"></p><p>注意一下端口号，别跟个 SB 一样，自己改了配置文件里的端口号，这里还是写 5001，你改的什么就用什么。</p><p>不出意外的话到这里教程就完成了。</p>',26),p=[c];function r(l,h){return a(),s("div",null,p)}const _=e(o,[["render",r],["__file","install-on-baota.html.vue"]]);export{_ as default};
